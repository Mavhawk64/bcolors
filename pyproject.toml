[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "bcolors"
version = "0.1.0"
description = "ANSI terminal color and style constants for Python"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.urls]
Repository = "https://github.com/Mavhawk64/bcolors"

[tool.setuptools]
py-modules = ["bcolors"]
